<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SazakyanApp" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Base application theme. -->
    <style name="Base.Theme.SazakyanApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.SazakyanSplashScreen" parent="Base.Theme.SazakyanApp"/>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/bg_splashscreen</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SazakyanApp</item>
    </style>
</resources>